// admin-area only javascript

// convert the primary category checkboxes into a set of radio buttons
jQuery("#primary_categorychecklist-pop input, #primary_categorychecklist input, .categorychecklist input").each(function(){this.type="radio"});

